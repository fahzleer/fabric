name: Keep-alive

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Neon via health check
        run: curl -sf https://fabric.cool/api/health?deep=true || true
